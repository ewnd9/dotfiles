## karabiner

- CapsLock to Hyper
  - capslock = esc(click) | hyper(hold)
  - escape = capslock switch
  - spacebar = language switch
  - command + spacebar = emoji
- Hyper Navigation
  - command + h = shift + left
  - option + command + h = option + shift + left (select word ahead)
  - shift + h = ctrl + shift + tab (prev tab)
  - control + h = ctrl + left (prev desktop)
  - option + h = mouse left
  - shift + option + h = mouse left fast
  - shift + control + h = mouse wheel left
  - shift + command + h = mouse wheel left fast
  -  h = left
  - command + j = shift + down
  - option + command + j = option + shift + down (select para ahead)
  - shift + j = command + tab (next application)
  - control + j = ctrl + down (focus application)
  - option + j = mouse down
  - shift + option + j = mouse down fast
  - shift + control + j = mouse wheel down
  - shift + command + j = mouse wheel down fast
  -  j = down
  - command + k = shift + up
  - option + command + k = option + shift + up (select line above)
  - shift + k = command + tab (prev application)
  - control + k = ctrl + up (expose all)
  - option + k = mouse up
  - shift + option + k = mouse up fast
  - shift + control + k = mouse wheel up
  - shift + command + k = mouse wheel up fast
  -  k = up
  - command + l = shift + right
  - option + command + l = option + shift + right (select word after)
  - shift + l = command + tab (next tab)
  - control + l = ctrl + right (next desktop)
  - option + l = mouse move right
  - shift + option + l = mouse move right fast
  - shift + control + l = mouse wheel right
  - shift + command + l = mouse wheel right fast
  -  l = right
  - command + u = shift + page_up
  - option + command + u = shift + page_up
  - shift + u = option + command + hyphen (zoom out)
  - control + u = ctrl + command + f (fullscreen)
  - option + u = mouse left click
  - shift + option + u = mouse left click
  - shift + control + u = mouse wheel left (rev)
  - shift + command + u = mouse wheel left (rev) fast
  -  u = page_up
  - command + i = shift + home
  - option + command + i = select whole line to head
  - shift + i = command + shift + ` (prev window within app)
  - control + i = command + h (hide current window)
  - option + i = mouse right click
  - shift + option + i = mouse move down fast
  - shift + control + i = mouse wheel down (rev)
  - shift + command + i = mouse wheel down (rev) fast
  -  i = home
  - command + o = shift + end
  - option + command + o = select whole
  - shift + o = command ` (next window within app)
  - control + o = option + command + h (expose all)
  - option + o = mouse backward
  - shift + option + o = mouse backward
  - shift + control + o = mouse wheel up (rev)
  - shift + command + o = mouse wheel up (rev) fast
  -  o = end
  - command + p = shift + page_down
  - option + command + p = shift + page_down
  - shift + p = option + command + equal_sign (zoom in)
  - control + p = LaunchPad
  - option + p = mouse forward
  - shift + option + p = mouse forward
  - shift + control + p = mouse wheel right (rev)
  - shift + command + p = mouse wheel right (rev) fast
  -  p = page_down
  - control + shift + enter = button1 (mouse left click)
  - enter = button1 (mouse left click)
  - command + enter = button2 (mouse right click)
  - option + enter = button3 (mouse middle click)
  - shift + enter = button4 (mouse back)
  - control + enter = button5 (mouse forward)
  - shift + up arrow = mouse wheel up
  - shift + down arrow = mouse wheel down
  - shift + left arrow = mouse wheel left
  - shift + right arrow = mouse wheel right
  - shift + option + up arrow = mouse wheel up slow
  - shift + option + down arrow = mouse wheel down slow
  - shift + option + left arrow = mouse wheel left slow
  - shift + option + right arrow = mouse wheel right slow
  - shift + command + up arrow = mouse wheel up fast
  - shift + command + down arrow = mouse wheel down fast
  - shift + command + left arrow = mouse wheel left fast
  - shift + command + right arrow = mouse wheel right fast
  - option + up arrow = mouse up slow
  - option + down arrow = mouse down slow
  - option + left arrow = mouse left slow
  - option + right arrow = mouse right slow
  - command + up arrow = mouse up fast
  - command + down arrow = mouse down fast
  - command + left arrow = mouse left fast
  - command + right arrow = mouse right fast
  - up arrow = mouse up
  - down arrow = mouse down
  - left arrow = mouse left
  - right arrow = mouse right
  - activate chrome
  - activate telegram
  - activate 1st workspace
  - activate 2nd workspace
  - activate 3rd workspace
  - activate 4th workspace
  - right_option as enter