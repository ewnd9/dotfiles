{
  "Check arguments count": {
    "prefix": "args",
    "body": "if [ \"$#\" -ne 1 ]; then\n  echo \"Usage: $1 <$2>\"\n  exit 1\nfi",
    "description": "Check arguments count"
  },
  "apt": {
    "prefix": "apt",
    "body": "sudo apt-get install $1",
    "description": "apt"
  }
}