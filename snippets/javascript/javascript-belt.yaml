lang: javascript
snippets:
  - name: belt
    prefix: belt
    body: |-
      'use strict';

      const execa = require('belt-tools/modules/execa');

      module.exports = {
        run
      };

      async function run({ argv }) {
      }

