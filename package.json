{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "babel-eslint": "^8.2.3",
    "eslint": "^4.19.1",
    "husky": "^0.14.3"
  },
  "scripts": {
    "precommit": "yarn lint && yarn lint:shell",
    "lint": "eslint 'packages/**/*.js'",
    "lint:shell": "belt lint-shell"
  }
}
